export enum MongoDBStatus {
  OK = "OK",
  DOWN = "DOWN",
}

export enum RedisStatus {
  OK = "OK",
  DOWN = "DOWN",
}

export enum RabbitMQStatus {
  OK = "OK",
  DOWN = "DOWN",
}

export enum HealthStatus {
  OK = "OK",
  ERROR = "ERROR",
}
